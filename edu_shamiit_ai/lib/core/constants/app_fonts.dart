class AppFonts {
  // Using system fonts as fallback since custom fonts aren't bundled yet
  // For production: download fonts from Google Fonts and add to pubspec.yaml
  static const String? heading = null; // Uses system default (Roboto on Android)
  static const String? body = null;    // Uses system default
}
